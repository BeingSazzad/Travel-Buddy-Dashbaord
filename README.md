# Seluna Admin Dashboard

Web admin for the Travel Buddy / Seluna member app. Separate repo, separate domain.

## Local

```bash
npm install
npm run dev
```

Demo login: `admin@seluna.app` / `admin123`

Frontend-only demo data (localStorage). No backend required.
